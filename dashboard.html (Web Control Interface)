<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pocket Option Trading Bot - Ultimate Dashboard v21.0</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            color: #eee;
            padding: 20px;
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
        }

        /* Header */
        .header {
            text-align: center;
            padding: 20px;
            background: linear-gradient(135deg, #0f3460, #1a1a2e);
            border-radius: 15px;
            margin-bottom: 20px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.3);
        }

        .header h1 {
            font-size: 2em;
            color: #ffd700;
        }

        .header p {
            color: #aaa;
            margin-top: 5px;
        }

        .status-badge {
            display: inline-block;
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 12px;
            margin-top: 10px;
        }

        .status-online {
            background: #00ff88;
            color: #1a1a2e;
        }

        /* Stats Grid */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-bottom: 20px;
        }

        .stat-card {
            background: rgba(255,255,255,0.1);
            border-radius: 10px;
            padding: 15px;
            text-align: center;
            backdrop-filter: blur(10px);
        }

        .stat-card h3 {
            font-size: 12px;
            color: #aaa;
            margin-bottom: 5px;
        }

        .stat-card .value {
            font-size: 24px;
            font-weight: bold;
            color: #ffd700;
        }

        /* Control Panel */
        .control-panel {
            background: rgba(255,255,255,0.05);
            border-radius: 15px;
            padding: 20px;
            margin-bottom: 20px;
        }

        .panel-title {
            font-size: 18px;
            margin-bottom: 15px;
            color: #ffd700;
            border-left: 3px solid #ffd700;
            padding-left: 10px;
        }

        .button-group {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            margin-bottom: 20px;
        }

        .btn {
            padding: 10px 20px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-weight: bold;
            transition: all 0.3s ease;
        }

        .btn-primary {
            background: #ffd700;
            color: #1a1a2e;
        }

        .btn-primary:hover {
            background: #ffed4a;
            transform: translateY(-2px);
        }

        .btn-danger {
            background: #dc3545;
            color: white;
        }

        .btn-danger:hover {
            background: #ff4757;
        }

        .btn-success {
            background: #00cc88;
            color: white;
        }

        .btn-success:hover {
            background: #00ffaa;
        }

        .btn-warning {
            background: #ff8800;
            color: white;
        }

        .btn-info {
            background: #17a2b8;
            color: white;
        }

        /* Timeframe Tabs */
        .timeframe-tabs {
            display: flex;
            flex-wrap: wrap;
            gap: 5px;
            margin-bottom: 20px;
            border-bottom: 1px solid #333;
            padding-bottom: 10px;
        }

        .tab {
            padding: 8px 16px;
            background: rgba(255,255,255,0.1);
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .tab.active {
            background: #ffd700;
            color: #1a1a2e;
        }

        .tab:hover:not(.active) {
            background: rgba(255,255,255,0.2);
        }

        /* Signals Table */
        .signals-container {
            background: rgba(0,0,0,0.3);
            border-radius: 15px;
            padding: 20px;
            margin-bottom: 20px;
        }

        .signals-table {
            width: 100%;
            border-collapse: collapse;
        }

        .signals-table th,
        .signals-table td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #333;
        }

        .signals-table th {
            background: rgba(255,215,0,0.1);
            color: #ffd700;
        }

        .signal-call {
            color: #00ff88;
            font-weight: bold;
        }

        .signal-put {
            color: #ff4757;
            font-weight: bold;
        }

        .confidence-high {
            color: #00ff88;
        }

        .confidence-mid {
            color: #ffd700;
        }

        .confidence-low {
            color: #ff8800;
        }

        /* Pair List */
        .pairs-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
            gap: 8px;
            max-height: 300px;
            overflow-y: auto;
            padding: 10px;
        }

        .pair-item {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 8px 12px;
            background: rgba(255,255,255,0.05);
            border-radius: 8px;
            font-size: 12px;
        }

        .pair-enabled {
            color: #00ff88;
        }

        .pair-disabled {
            color: #ff4757;
        }

        .pair-toggle {
            width: 40px;
            padding: 4px 8px;
            font-size: 10px;
            border-radius: 4px;
            cursor: pointer;
        }

        /* Auto-scan Status */
        .autoscan-grid {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            margin-top: 10px;
        }

        .autoscan-item {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 8px 15px;
            background: rgba(255,255,255,0.05);
            border-radius: 8px;
        }

        .autoscan-active {
            color: #00ff88;
        }

        .autoscan-inactive {
            color: #ff4757;
        }

        /* Logs */
        .logs-container {
            background: #0a0a0a;
            border-radius: 15px;
            padding: 20px;
            max-height: 300px;
            overflow-y: auto;
            font-family: monospace;
            font-size: 12px;
        }

        .log-entry {
            padding: 4px 0;
            border-bottom: 1px solid #222;
            color: #aaa;
        }

        .log-signal {
            color: #ffd700;
        }

        /* Loading */
        .loading {
            text-align: center;
            padding: 20px;
            color: #aaa;
        }

        /* Refresh button */
        .refresh-btn {
            float: right;
            background: transparent;
            border: 1px solid #ffd700;
            color: #ffd700;
            padding: 5px 10px;
            border-radius: 5px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <h1>🏆 POCKET OPTION TRADING BOT v21.0</h1>
            <p>Ultimate Dashboard - Live Trading Signals | 15 MINUTE PRIMARY</p>
            <div class="status-badge status-online" id="botStatus">🟢 BOT ONLINE</div>
        </div>

        <!-- Stats -->
        <div class="stats-grid">
            <div class="stat-card">
                <h3>📊 SIGNALS TODAY</h3>
                <div class="value" id="totalSignals">0</div>
            </div>
            <div class="stat-card">
                <h3>📈 CALL SIGNALS</h3>
                <div class="value" id="callSignals">0</div>
            </div>
            <div class="stat-card">
                <h3>📉 PUT SIGNALS</h3>
                <div class="value" id="putSignals">0</div>
            </div>
            <div class="stat-card">
                <h3>⭐ AVG CONFIDENCE</h3>
                <div class="value" id="avgConfidence">0%</div>
            </div>
            <div class="stat-card">
                <h3>👥 ENABLED PAIRS</h3>
                <div class="value" id="enabledPairs">0</div>
            </div>
            <div class="stat-card">
                <h3>🔄 ACTIVE SCANS</h3>
                <div class="value" id="activeScans">0</div>
            </div>
        </div>

        <!-- Control Panel -->
        <div class="control-panel">
            <div class="panel-title">🎮 CONTROL CENTER</div>
            <div class="button-group">
                <button class="btn btn-primary" onclick="sendCommand('/manual_on')">🟢 MANUAL SCAN ON</button>
                <button class="btn btn-danger" onclick="sendCommand('/manual_off')">🔴 MANUAL SCAN OFF</button>
                <button class="btn btn-success" onclick="sendCommand('/startall')">▶️ START ALL AUTO-SCANS</button>
                <button class="btn btn-warning" onclick="sendCommand('/stopall')">⏸️ STOP ALL AUTO-SCANS</button>
                <button class="btn btn-info" onclick="sendCommand('/scan')">🔍 SCAN NOW (15m)</button>
                <button class="btn btn-primary" onclick="sendCommand('/enable_all')">✅ ENABLE ALL PAIRS</button>
                <button class="btn btn-danger" onclick="sendCommand('/disable_all')">❌ DISABLE ALL PAIRS</button>
            </div>
        </div>

        <!-- Timeframe Tabs -->
        <div class="control-panel">
            <div class="panel-title">⏰ TIMEFRAME SELECTOR</div>
            <div class="timeframe-tabs" id="timeframeTabs">
                <div class="tab" data-tf="1m">1 MINUTE</div>
                <div class="tab" data-tf="5m">5 MINUTE</div>
                <div class="tab active" data-tf="15m">15 MINUTE ⭐ PRIMARY</div>
                <div class="tab" data-tf="30m">30 MINUTE</div>
                <div class="tab" data-tf="1h">1 HOUR</div>
                <div class="tab" data-tf="4h">4 HOUR</div>
            </div>
            <div class="button-group" style="margin-top: 15px;">
                <button class="btn btn-primary" onclick="sendCommandWithTF('/scan')">🔍 SCAN SELECTED TIMEFRAME</button>
                <button class="btn btn-success" onclick="sendCommandWithTF('/start')">▶️ START AUTO-SCAN</button>
                <button class="btn btn-warning" onclick="sendCommandWithTF('/stop')">⏸️ STOP AUTO-SCAN</button>
            </div>
        </div>

        <!-- Signals Display -->
        <div class="signals-container">
            <div style="overflow: hidden; margin-bottom: 15px;">
                <div class="panel-title" style="float: left;">📡 LIVE SIGNALS</div>
                <button class="refresh-btn" onclick="refreshData()">🔄 REFRESH</button>
            </div>
            <div id="signalsContent">
                <div class="loading">Loading signals...</div>
            </div>
        </div>

        <!-- Auto-scan Status -->
        <div class="control-panel">
            <div class="panel-title">🔄 AUTO-SCAN STATUS</div>
            <div class="autoscan-grid" id="autoscanStatus">
                <div class="loading">Loading...</div>
            </div>
        </div>

        <!-- Pair Selection -->
        <div class="control-panel">
            <div class="panel-title">👥 PAIR SELECTION (Click to toggle)</div>
            <div id="pairsList">
                <div class="loading">Loading pairs...</div>
            </div>
        </div>

        <!-- Logs -->
        <div class="control-panel">
            <div class="panel-title">📋 ACTIVITY LOG</div>
            <div class="logs-container" id="logs">
                <div class="log-entry">🚀 Bot started - Waiting for signals...</div>
            </div>
        </div>
    </div>

    <script>
        // API Configuration
        const API_BASE = 'http://localhost:3000';
        let currentTimeframe = '15m';
        let refreshInterval = null;

        // Initialize
        document.addEventListener('DOMContentLoaded', () => {
            setupTimeframeTabs();
            refreshData();
            startAutoRefresh();
            fetchLogs();
        });

        function setupTimeframeTabs() {
            document.querySelectorAll('.tab').forEach(tab => {
                tab.addEventListener('click', () => {
                    document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));
                    tab.classList.add('active');
                    currentTimeframe = tab.dataset.tf;
                    refreshData();
                });
            });
        }

        function startAutoRefresh() {
            if (refreshInterval) clearInterval(refreshInterval);
            refreshInterval = setInterval(() => {
                refreshData();
                fetchLogs();
            }, 5000);
        }

        async function refreshData() {
            await fetchSignals();
            await fetchStatus();
            await fetchPairs();
            await fetchAutoscanStatus();
        }

        async function fetchSignals() {
            try {
                const response = await fetch(`${API_BASE}/api/signals?tf=${currentTimeframe}`);
                const data = await response.json();
                displaySignals(data);
                updateStats(data);
            } catch(e) {
                console.error('Error fetching signals:', e);
                document.getElementById('signalsContent').innerHTML = '<div class="loading">⚠️ Cannot connect to bot. Make sure bot is running.</div>';
            }
        }

        function displaySignals(signals) {
            const container = document.getElementById('signalsContent');
            
            if (!signals || signals.length === 0) {
                container.innerHTML = '<div class="loading">📭 No signals yet. Click SCAN NOW to generate signals.</div>';
                return;
            }

            let html = `<table class="signals-table">
                <thead>
                    <tr><th>PAIR</th><th>SIGNAL</th><th>CONF</th><th>PROB</th><th>RSI</th><th>ADX</th><th>TREND</th><th>EXPIRY</th><th>TIME</th></tr>
                </thead>
                <tbody>`;

            for (const signal of signals) {
                const signalClass = signal.signal === 'CALL' ? 'signal-call' : 'signal-put';
                const confidenceClass = signal.confidence >= 75 ? 'confidence-high' : (signal.confidence >= 60 ? 'confidence-mid' : 'confidence-low');
                const trendEmoji = signal.trendAlignment === 'WITH TREND ✅' ? '✅' : (signal.trendAlignment === 'AGAINST TREND ⚠️' ? '⚠️' : '⚪');
                
                html += `<tr>
                    <td><strong>${signal.pair}</strong></td>
                    <td class="${signalClass}">${signal.signal}</td>
                    <td class="${confidenceClass}">${signal.confidence}%</td>
                    <td>${signal.probability || signal.confidence}%</td>
                    <td>${signal.rsi || 'N/A'}</td>
                    <td>${signal.adx || 'N/A'}</td>
                    <td>${trendEmoji} ${signal.trendAlignment || 'N/A'}</td>
                    <td>${signal.expiry || '15'}min</td>
                    <td>${signal.time || new Date().toLocaleTimeString()}</td>
                </tr>`;
            }

            html += `</tbody></table>`;
            container.innerHTML = html;
        }

        function updateStats(signals) {
            const total = signals.length;
            const calls = signals.filter(s => s.signal === 'CALL').length;
            const puts = signals.filter(s => s.signal === 'PUT').length;
            const avgConf = signals.length > 0 ? Math.round(signals.reduce((sum, s) => sum + s.confidence, 0) / signals.length) : 0;
            
            document.getElementById('totalSignals').innerText = total;
            document.getElementById('callSignals').innerText = calls;
            document.getElementById('putSignals').innerText = puts;
            document.getElementById('avgConfidence').innerText = avgConf + '%';
        }

        async function fetchStatus() {
            try {
                const response = await fetch(`${API_BASE}/api/status`);
                const data = await response.json();
                document.getElementById('enabledPairs').innerText = data.enabledPairs || 0;
                document.getElementById('activeScans').innerText = data.activeScans || 0;
            } catch(e) {}
        }

        async function fetchPairs() {
            try {
                const response = await fetch(`${API_BASE}/api/pairs`);
                const data = await response.json();
                displayPairs(data);
            } catch(e) {}
        }

        function displayPairs(pairs) {
            const container = document.getElementById('pairsList');
            if (!pairs || pairs.length === 0) {
                container.innerHTML = '<div class="loading">No pairs data</div>';
                return;
            }

            let html = '<div class="pairs-grid">';
            for (const pair of pairs) {
                const statusClass = pair.enabled ? 'pair-enabled' : 'pair-disabled';
                const statusText = pair.enabled ? '✅' : '❌';
                html += `<div class="pair-item">
                    <span class="${statusClass}">${statusText} ${pair.name}</span>
                    <button class="pair-toggle btn-info" onclick="togglePair('${pair.name}', ${!pair.enabled})">${pair.enabled ? 'DISABLE' : 'ENABLE'}</button>
                </div>`;
            }
            html += '</div>';
            container.innerHTML = html;
        }

        async function fetchAutoscanStatus() {
            try {
                const response = await fetch(`${API_BASE}/api/autoscan`);
                const data = await response.json();
                displayAutoscanStatus(data);
            } catch(e) {}
        }

        function displayAutoscanStatus(scans) {
            const container = document.getElementById('autoscanStatus');
            if (!scans || scans.length === 0) {
                container.innerHTML = '<div class="loading">No auto-scan data</div>';
                return;
            }

            let html = '';
            for (const scan of scans) {
                const statusClass = scan.running ? 'autoscan-active' : 'autoscan-inactive';
                const statusText = scan.running ? '🟢 RUNNING' : '🔴 STOPPED';
                html += `<div class="autoscan-item">
                    <span class="${statusClass}">📊 ${scan.name}</span>
                    <span>${statusText}</span>
                    <button class="btn-info" style="padding:4px 8px;font-size:10px;" onclick="sendCommand('/${scan.running ? 'stop' : 'start'} ${scan.tf}')">${scan.running ? 'STOP' : 'START'}</button>
                </div>`;
            }
            container.innerHTML = html;
        }

        async function fetchLogs() {
            try {
                const response = await fetch(`${API_BASE}/api/logs`);
                const data = await response.json();
                displayLogs(data);
            } catch(e) {}
        }

        function displayLogs(logs) {
            const container = document.getElementById('logs');
            if (!logs || logs.length === 0) return;
            
            let html = '';
            for (const log of logs.slice(-50)) {
                const logClass = log.includes('SIGNAL') ? 'log-signal' : '';
                html += `<div class="log-entry ${logClass}">${log}</div>`;
            }
            container.innerHTML = html;
            container.scrollTop = container.scrollHeight;
        }

        function sendCommand(command) {
            fetch(`${API_BASE}/api/command`, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ command: command })
            }).then(() => {
                setTimeout(refreshData, 1000);
            }).catch(e => console.error('Error sending command:', e));
        }

        function sendCommandWithTF(command) {
            sendCommand(`${command} ${currentTimeframe}`);
        }

        function togglePair(pairName, enable) {
            const command = enable ? `/enable ${pairName}` : `/disable ${pairName}`;
            sendCommand(command);
        }
    </script>
</body>
</html>
