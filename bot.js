// OMNI BULLS EYE PROTOCOL v6.0 — FINAL PRODUCTION // Target Rating: 4.6 / 5 // Features: Real data · Multi-TF · Divergence · Volume · News filter // Backtest · Circuit breaker · Session gate · Live priority // ═══════════════════════════════════════════════════════════════ const TelegramBot = require('node-telegram-bot-api'); const { fetchPriceData, fetchHistoricalData, isNewsBlackout } = require('./priceFetcher'); const { analyzeSignal, backtest } = require('./analyzer'); const token = process.env.TELEGRAM_BOT_TOKEN; const CHAT_ID = process.env.TELEGRAM_CHAT_ID; if (!token || !CHAT_ID) { console.error('❌ TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_
